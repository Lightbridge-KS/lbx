% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize.R
\name{get_custom_summary_stats}
\alias{get_custom_summary_stats}
\title{Compute Custom Summary Statistics for One or More Variables}
\usage{
get_custom_summary_stats(df, ..., show = NULL)
}
\arguments{
\item{df}{A \code{data.frame} or \code{tibble}. The input dataset.}

\item{...}{One or more variable names (unquoted or quoted) for which the summary statistics
will be computed.}

\item{show}{A character vector specifying which summary statistics to include in the output.
Possible values are \code{"n"}, \code{"mean"}, \code{"sd"}, \code{"iqr"}, \code{"median"}, \code{"skewness"},
and \code{"kurtosis"}. If \code{NULL} (default), all statistics are included.}
}
\value{
A \code{tibble} containing the computed summary statistics. Each statistic is presented
as a column with names formatted as \verb{<variable>_<statistic>}.
}
\description{
The \code{get_custom_summary_stats()} function computes a set of summary statistics
(e.g., count, mean, standard deviation, interquartile range, median, skewness, and kurtosis)
for one or more variables in a given data frame. Users can optionally specify which statistics
to display.
}
\details{
This function uses \code{dplyr::across()} to apply multiple summary functions dynamically
to the specified variables. Summary statistics are computed using base R and
the \code{moments} package for skewness and kurtosis. The resulting summary statistics
are named dynamically as \verb{<variable>_<statistic>}.
}
\examples{
library(dplyr)
library(moments)

# Example data
df <- tibble::tibble(
  Score = c(85, 90, 78, NA, 92),
  Age = c(25, 30, 35, 40, 45)
)

# Compute all summary statistics for `Score` and `Age`
get_custom_summary_stats(df, Score, Age)

# Compute only mean and standard deviation for `Score`
get_custom_summary_stats(df, Score, show = c("mean", "sd"))

}
